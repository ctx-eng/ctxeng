"""Observability and context tracing.

* ``ContextTracer`` — instruments the assembly pipeline (retrieve → dedup → prioritize → assemble → trim)
* ``TraceReporter`` — human-readable trace formatting
"""
