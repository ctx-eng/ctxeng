"""Retrieval layer for semantic and hybrid search.

* ``EmbeddingModel`` — sentence-transformers wrapper (optional dep)
* ``HybridRetriever`` — dense + BM25 + keyword scoring with configurable alpha
* ``CrossEncoderReranker`` — cross-encoder re-ranking (optional dep)
"""
