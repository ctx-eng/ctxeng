"""Agentic context routing.

* ``ContextRouter`` — multi-step session tracking with diff computation
* ``ContextDiff`` — added / removed / score-changed items between steps
* ``LifecycleManager`` — born → active → stale → archived → dead state machine
"""
